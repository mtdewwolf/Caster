services:
  caster:
    container_name: caster
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: caster:latest
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - HOST=0.0.0.0
      - MEDIA_DATA_DIR=/app/data
      - THUMBNAILS_DIR=/app/data/thumbnails
      - TRANSCODE_CACHE_DIR=/app/data/transcode_cache
      - TRANSCODE_MAX_CONCURRENT=${TRANSCODE_MAX_CONCURRENT:-2}
      - TRANSCODE_CACHE_MAX_AGE_HOURS=${TRANSCODE_CACHE_MAX_AGE_HOURS:-24}
      - TRANSCODE_CACHE_MAX_SIZE_MB=${TRANSCODE_CACHE_MAX_SIZE_MB:-10000}
      # Optional: exact IP/CIDR allowlist if first-run setup is performed
      # outside the built-in LAN/private ranges.
      # - CASTER_SETUP_NETWORKS=100.64.0.0/10
      # Set timezone matching your TrueNAS SCALE host
      - TZ=America/New_York
    volumes:
      # Persistent app data, database, posters, and transcoder cache
      - /mnt/tank/appdata/caster/data:/app/data
      # Your TrueNAS ZFS media pool (Read-Only)
      - /mnt/tank/media:/media:ro
    
    # ----------------------------------------------------
    # Hardware Acceleration for TrueNAS SCALE
    # ----------------------------------------------------
    # For Intel QuickSync & AMD VAAPI (Integrated Graphics):
    devices:
      - /dev/dri:/dev/dri
    group_add:
      - "44" # Video group ID (typical on Debian/TrueNAS)
      - "107" # Render group ID

    # For NVIDIA GPU (Uncomment if using an NVIDIA card in TrueNAS SCALE):
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: all
    #           capabilities: [gpu, video]
    # environment:
    #   - NVIDIA_VISIBLE_DEVICES=all
    #   - NVIDIA_DRIVER_CAPABILITIES=all,video,compute,utility
